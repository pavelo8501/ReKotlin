package po.auth.classes


//import kotlinx.coroutines.*
//import kotlin.coroutines.*

/**
 * Utility for manually attaching a session to a Ktor call.
 */
object SessionAttachment {
//    val UserSessionKey = AttributeKey<UserSession>("user-session")
//
//    fun ApplicationCall.attachSession(session: UserSession) {
//        attributes.put(UserSessionKey, session)
//    }
//
//    fun ApplicationCall.getAttachedSession(): UserSession? {
//        return attributes.getOrNull(UserSessionKey)
//    }
}
