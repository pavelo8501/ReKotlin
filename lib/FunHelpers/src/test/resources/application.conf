
app {
  assetsPath = "/var/data/assets"
  optional = "Optional value"
  nullableParam = null
  categories = ["photo", "html", "document"]
  nested {
    requestTimeOut = 60000
    socketTimeout = 60000
    number = 42
    boolean = true
  }
}

test{
  number =  300
  string = "some string"
}

test_null{
  number =  null
  string = "some string"
}

test_records {
  records =  ["photo", "html", "document"]
}

test_null_records {
  records =  null
}

test_nested {
  string = "some string"
  nested =  {
    nestedProperty = "some nested string"
  }
}

test_nested_nullable {
  string = "some string"
  nested = null
}
